export enum Path {
  DASHBOARD = '/',
  SETTINGS = '/settings',
  NOT_FOUND = '/not-found',
  TLS_COMMUNICATIONS = '/tls-communications',
  CERTIFICATES = '/certificates',
  UNDEFINED = '*',
}
